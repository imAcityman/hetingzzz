import { InputIosPopupDirective } from './input-ios-popup.directive';

describe('InputIosPopupDirective', () => {
  it('should create an instance', () => {
    const directive = new InputIosPopupDirective();
    expect(directive).toBeTruthy();
  });
});
